[package]
name = "netzprobe-api"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "*"
serde = { version = "*", features = ["derive"] }
serde_json = "*"
tokio = { version = "*", features = ["macros", "rt-multi-thread", "net", "signal"] }
chrono = "*"
chrono-tz = "*"
