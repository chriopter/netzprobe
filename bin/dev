#!/usr/bin/env bash
# Startet API und Vite-Dev-Server.
set -euo pipefail
cd "$(dirname "$0")/.."

bin/api &
api_pid="$!"

cleanup() {
  kill "$api_pid" 2>/dev/null || true
}
trap cleanup EXIT INT TERM

npm run vite
