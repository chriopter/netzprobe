[package]
name = "netzprobe-kern"
version = "0.1.0"
edition = "2024"

[lib]
path = "model.rs"

[dependencies]
chrono = "*"
chrono-tz = "*"
serde = { version = "*", features = ["derive"] }
serde_json = "*"
